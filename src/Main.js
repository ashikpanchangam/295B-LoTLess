import React, { Component } from 'react';
import RouterComponent from './Router';

class Main extends Component {
  render() {
      return (
         <RouterComponent />
    );
  }
}

export default Main;
